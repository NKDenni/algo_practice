function solution(inputString, width) {
    let window = "";
    let len = inputString.length;

    for(let i = 0; i < len; i++){
        // if current inputString[i] is not in window append to window
        // if current inputString[i] is contained in window 
    }

}
